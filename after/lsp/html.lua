-- html: HTML LSP
return {
    settings = {
        html = {
            format = {
                enable = true,
            },
            hover = {
                documentation = true,
                references = true,
            },
        },
    },
}
